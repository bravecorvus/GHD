#include <stdio.h>
#include <stdlib.h>

int main() {
    unsigned int *N;
    N = (int*) malloc(sizeof(int));
    printf("How many integers do you want to input?");
    scanf("%d", N);
    for(int i = 0; i < *N, ++i) {

    }
}